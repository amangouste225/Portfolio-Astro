---
interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a href={href} class="link"><slot /></a>

<style>
	a {
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
		color: var(--black-0);
		text-decoration: none;
		line-height: 1;
		gap: 1rem;
		overflow: hidden;
		white-space: nowrap;
		border:  2px solid;
		border-radius: 50px;
	}

	@media (min-width: 20em) {
		a {
			font-size: var(--text-md);
		}
	}


	a:focus,
	a:hover {
		background-color: var(--accent-regular);
		border: none;
		color: var(--black-0);
	}

	@media (min-width: 50em) {
		a {
			padding: 1rem 0.5rem;
			font-size: var(--text-md);
			font-weight: 600;
			max-width: 200px;
		}
	}
</style>
