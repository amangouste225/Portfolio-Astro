---

interface Props {
	name : string;
    para?: string;
	align ?: 'start' | undefined
}

const {name, para } = Astro.props;
---

<div class="flex flex-col items-center gap-3 justify-center my-20">
	<h2> {name}</h2>
	<slot />
</div>