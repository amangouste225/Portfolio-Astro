---

interface Props {
	name : string;
    para : string;
}

const {name, para } = Astro.props;
---

<div class="flex flex-col items-center justify-center my-10">
	<h1 class="greetings"> {name}</h1>
	<p class="title">{para} </p>
	<slot />
</div>